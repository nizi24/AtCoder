i = -2; i <= 2; i++) {
		// 	for (int j = -2; j <= 2; j++) {
		// 		if (abs(i) + abs(j) <= 1) continue;
		// 		if (y + i >= H && y + i < 0) continue;
		// 		if (x + i >= W && x + i < 0) continue;
		// 		if (S[y + i][x + i] == '#') continue;

		// 		if (chmin(dist[y + i][x + i], d + 1)) {
		// 			que.push({d + 1, {y + i, x + i}});
		// 		}
		// 	}
		// }